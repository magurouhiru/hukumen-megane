# ローカルでの確認用
# serve.bat から参照される nginx の設定ファイル
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}